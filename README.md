# spring-boot-count-words
A Spring Boot application that processes a List of Strings and applies the following business rules: - 1.Counts and returns the NUMBER of words (i.e. Strings) that start with "M" or "m" - 2.Returns all the words longer than 5 characters.
